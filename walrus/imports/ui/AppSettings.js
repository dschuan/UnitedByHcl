import React, { Component } from 'react';

export default class AppSettings extends Component {
    render() {
        return (
            <button className="btn btn-default app-header-icon">
                <span className="glyphicon glyphicon-cog white"></span>
            </button>
        );
    }
}
